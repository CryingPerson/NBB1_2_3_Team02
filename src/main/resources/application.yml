
spring:
  profiles:
    active:
    include : secret
  threads:
    virtual:
      enabled: true
  datasource:
    url: jdbc:mysql://database-elastic.c74kmgiikxyu.ap-northeast-2.rds.amazonaws.com:3306/test_db
    #    DB_CLOSE_ON_EXIT=FALSE # MySQL 모드 설정
    username: test
    password: 1234
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_batch_fetch_size: 100
        format_sql: true
        highlight_sql: true
        use_sql_comments: true
        hibernate.generate_statistics: true
        jdbc:
          batch_size: 1000
        order_inserts : true
        order_updates:  true
      show_sql: true

logging:
  level:
    com.example.kotlindevelop: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.hibernate.orm.jdbc.extract: TRACE
    org.springframework.transaction.interceptor: TRACE
server:
  port: 5000